const { Pool } = require('pg'); const pool = new Pool({user: 'thomaspoole', host: 'localhost', database: 'inventory_production_db', password: '3414', port: 5432}); async function run() { try { const res = await pool.query('SELECT * FROM skumapping LIMIT 5'); console.log('Sample rows:'); console.log(res.rows); } catch (err) { console.error(err); } finally { pool.end(); } } run();
