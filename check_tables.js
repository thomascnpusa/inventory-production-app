require('dotenv').config(); const { Pool } = require('pg'); const pool = new Pool({user: process.env.PG_USER, host: process.env.PG_HOST, database: process.env.PG_DATABASE, password: process.env.PG_PASSWORD, port: process.env.PG_PORT}); async function run() { try { const res = await pool.query('SELECT table_name FROM information_schema.tables WHERE table_schema=\'public\' AND table_type=\'BASE TABLE\';'); console.log(res.rows); const skuRes = await pool.query('SELECT * FROM "SkuMapping" LIMIT 5'); console.log('Sample SKU Mappings:'); console.log(skuRes.rows); } catch (err) { console.error(err); } finally { pool.end(); } } run();
